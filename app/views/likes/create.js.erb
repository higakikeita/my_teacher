$('#like-icon-post-<%= @teacher.id.to_s %>').
  html('<%= link_to "いいね", teacher_like_path(@teacher.id, @like), method: :DELETE, remote: true, class: "loved hide-text" %>');
$('#like-text-post-<%= @teacher.id.to_s %>').
  html('<%= j render "likess/like", { likes: @teacher.likes } %>');
